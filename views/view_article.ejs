<!DOCTYPE html>
<html lang="ko">
	<head>
		<meta charset="UTF-8" />
		<link rel="stylesheet" href="/public/article_styles.css" />
		<link rel="icon" type="image/x-icon" href="favicon.png" />
		<title><%= article.title %></title>
	</head>
	<body>
		<%-include("header.ejs") %>
		<h1><%= article.title %></h1>
		<p><strong>작성자:</strong> <%= article.author_name %></p>
		<p><strong>카테고리:</strong> <%= article.category %></p>
		<p><strong>업로드 시간:</strong> <%= article.createdAtFormatted %></p>
		<!-- 서버에서 넘겨준 시간 차이 표시 -->
		<p><strong>조회수:</strong> <%= article.views %></p>
        <% if (article.author_id === userId) { %>
            <a href="/articles/<%= article.id %>/edit">Edit</a>
            <form action="/articles/<%= article.id %>/delete" method="POST" style="display:inline;">
                <button type="submit">Delete</button>
            </form>
        <% } %>
		<p><%= article.content %></p>
	</body>
</html>
<button onclick="copyLink()">기사 공유하기</button>

<script>
function copyLink() {
    const link = window.location.href;
    navigator.clipboard.writeText(link).then(() => {
        alert('Link copied to clipboard!');
    }).catch(err => {
        console.error('Could not copy link: ', err);
    });
}
</script>

</body>
</html>
